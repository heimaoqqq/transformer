# VQ-VAE + Transformer 微多普勒生成系统依赖
# 基于diffusers和transformers的官方支持
# ⚠️ 重要：使用经过测试的兼容版本组合

# 核心深度学习框架 (优先安装)
torch==2.1.0+cu118
torchvision==0.16.0+cu118
torchaudio==2.1.0+cu118
--extra-index-url https://download.pytorch.org/whl/cu118

# HuggingFace生态系统 (diffusers官方指定配置)
# 官方安装命令: pip install diffusers[torch] transformers
# 注意：使用diffusers官方指定的版本组合，确保API兼容性
diffusers                        # 最新版本 (0.34.0，VQModel仍然可用)
transformers                     # 官方要求的依赖
huggingface-hub                  # 自动安装兼容版本
tokenizers                       # 自动安装兼容版本
safetensors                      # 稳定版本
accelerate                       # 加速库

# 数据处理
numpy==1.24.3
pillow==10.0.1
opencv-python==4.8.1.78
matplotlib==3.7.2
scikit-image==0.21.0

# 机器学习工具
scikit-learn==1.3.0
scipy==1.11.4
einops==0.7.0

# 训练工具
tqdm==4.66.1
tensorboard==2.15.1
packaging==23.2

# 感知损失 (VQ-VAE训练可选)
lpips==0.1.4

# 可选依赖
# wandb==0.16.0  # 实验跟踪
# xformers==0.0.22  # 内存优化 (需要CUDA)
# pytorch-fid==0.3.0  # FID评估
