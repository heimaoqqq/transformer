# VQ-VAE + Transformer 微多普勒生成系统依赖
# 基于diffusers和transformers的官方支持
# ⚠️ 重要：使用经过测试的兼容版本组合

# 核心深度学习框架 (优先安装)
torch==2.1.0+cu118
torchvision==0.16.0+cu118
torchaudio==2.1.0+cu118
--extra-index-url https://download.pytorch.org/whl/cu118

# HuggingFace生态系统 (兼容版本组合)
# 注意：这些版本经过测试，确保cached_download等API可用
huggingface-hub==0.17.3     # 支持cached_download的最后版本
tokenizers==0.14.1          # 兼容版本
safetensors==0.4.0          # 稳定版本
transformers==4.35.2        # 兼容版本
accelerate==0.24.1          # 兼容版本
diffusers==0.24.0           # 兼容版本

# 数据处理
numpy==1.24.3
pillow==10.0.1
opencv-python==4.8.1.78
matplotlib==3.7.2
scikit-image==0.21.0

# 机器学习工具
scikit-learn==1.3.0
scipy==1.11.4
einops==0.7.0

# 训练工具
tqdm==4.66.1
tensorboard==2.15.1
packaging==23.2

# 感知损失 (VQ-VAE训练可选)
lpips==0.1.4

# 可选依赖
# wandb==0.16.0  # 实验跟踪
# xformers==0.0.22  # 内存优化 (需要CUDA)
# pytorch-fid==0.3.0  # FID评估
